#include<stdio.h>
int fact(int a);
int main()
{
    int n,c;
    printf("Enter a number");
    scanf("%d",&n);
    c=fact(n);
    printf("factorial of %d is %d",n,c);
    
}

int fact(int a)
{
    if(a==0)
    return 0;
    if(a==1)
    return 1;
    else
    return a*fact(a-1);
     
}